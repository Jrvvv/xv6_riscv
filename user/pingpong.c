#include "kernel/types.h"
#include "user/user.h"
#include "kernel/fcntl.h"
#include "kernel/stat.h"

// int ping();
// int pong();

int main(int argc, char** argv) {
  // int ping_fd[2];
  // int pong_fd[2];

  exit(0);
}